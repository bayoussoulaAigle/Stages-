{% extends 'base.html.twig' %}

{% block title %}Login !{% endblock %}

{% block body %}
    <br><br><br>
    <style>body { background-color: #000000; } </style>
    <div class="jumbotron">
        <form method="post">
            {% if error %}
                <div class="alert alert-danger">{{ error.messageKey|trans(error.messageData, 'security') }}</div>
            {% endif %}

        
                <center><h6 class="display-3">Espace d'abonnés ✅ </h6></center>
                <label for="inputEmail" class="sr-only">Email</label>
                <input type="email" value="{{ last_username }}" name="email" id="inputEmail" class="form-control" placeholder="Email" required autofocus>
                <label for="inputPassword" class="sr-only">Password</label>
                <input type="password" name="password" id="inputPassword" class="form-control" placeholder="Password" required>

                <input type="hidden" name="_csrf_token"
                    value="{{ csrf_token('authenticate') }}"
                >

                {#
                    Uncomment this section and add a remember_me option below your firewall to activate remember me functionality.
                    See https://symfony.com/doc/current/security/remember_me.html

                    <div class="checkbox mb-3">
                        <label>
                            <input type="checkbox" name="_remember_me"> Remember me
                        </label>
                    </div>
                #}
                <p class="lead"><center>
                    <a class="btn btn-primary btn-lg" href="{{path('security_choixFormulaire')}}" role="button">Inscription </a>
                <button class="btn btn-lg btn-success" type="submit"> Connexion  </button><p></p>
                </center></p>
                <P></P>
                <center><p> Si vous n'etes pas encore membre du StageAlternance_Univ-orleans nous vous invitons à vous inscrire </P></cenetr>
        </form>
    
    </div>
{% endblock %}
